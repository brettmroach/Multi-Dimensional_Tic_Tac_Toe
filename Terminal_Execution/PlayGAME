
# Uses ttt_source.sh to play the game

dir=~/Tic\ Tac\ Toe

source "$dir"/Terminal_Execution/ttt_source.sh
echo "game: $GAME"

make -C "$dir/TTTfiles" $GAME

clear

"$dir/TTTfiles/$GAME" $size

echo "\n"

make -C "$dir"/TTTfiles clean

/bin/rm -f "$dir"/Terminal_Execution/ttt_source.sh

exit
